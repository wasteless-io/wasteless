# Sources de dependances (contraintes >=, editees a la main). Ce qui est
# REELLEMENT installe (install.sh + CI) est requirements.lock, un lock epingle
# et reproductible. Apres toute edition ici, regenerer le lock :
#   uv pip compile requirements.txt --universal --python-version 3.11 --output-file requirements.lock
# (--python-version 3.11 = plancher du CI ; sans lui uv prend le Python local
# et le lock peut perdre les forks 3.11 comme numpy/tomli)
boto3>=1.34.0
pandas>=2.2.3
numpy>=1.26.4
psycopg2-binary>=2.9.10
python-dotenv>=1.0.0
pyyaml>=6.0.1
click>=8.1.7

# AI insights / Daily Briefing (any litellm provider). The features stay
# hidden without WASTELESS_LLM_MODEL in .env, but the package is always
# installed: when it was optional, every venv rebuild silently dropped it
# and all AI features vanished from the UI without an error anywhere.
litellm>=1.40

# anyio est transitif (httpx, openai via litellm) mais doit rester aligne
# avec le lock UI, ou starlette (fastapi) le plafonne a 4.14.1 : la CI
# installe requirements.lock ET ui/requirements.lock dans le meme env, et
# deux versions d'anyio y sont irreconciliables. Sans starlette ici, la
# racine choisirait sinon 4.14.2 toute seule.
anyio<4.14.2

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
# moto mocks the AWS API in-process so the remediation write path (stop/start,
# describe) is exercised at every PR without real credentials — see
# tests/integration/test_ec2_remediator_moto.py.
moto[ec2]>=5.0.0
